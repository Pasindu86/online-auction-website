import RegisterForm from '../../components/auth/RegisterForm';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: 'Sign Up - Online Auction System',
  description: 'Create your account to start bidding on exclusive auctions',
};
